import { Container } from '../';

Контейнер - это самый основной элемент макета,
он центрирует содержимое по горизонтали и добавляет горизонтальные отступы из темы.

```tsx live
<Container size='md'>
    Default container
</Container>
```
